CHILL ZONE ZENITH - FIX 16
CREATIVE TAB COMPILE FIX

This is the SAME MOD moving forward from Fix 15.

ERROR FIXED
===========
ZenithCreativeTabExtras.java:
    CreativeModeTabEvents.modifyEntries(ZenithItems.ZENITH_CREATIVE_TAB_KEY)

Your Fabric API version does not expose modifyEntries(ResourceKey<CreativeModeTab>)
with that signature.

Fix 16 now passes the actual registered CreativeModeTab instance instead:
    CreativeModeTabEvents.modifyEntries(ZenithItems.ZENITH_CREATIVE_TAB)

NOTHING ELSE WAS REMOVED.
Fix 16 still includes:
- dedicated Zenith creative section
- /zenith detect <category>
- /zenith resetboss <category>
- /zenith give <targets> <item> <count>
- custom sword names, colours, lore and descriptions
- visual vanilla-style cooldown overlay
- Warden delayed beam prototype
- 15-lane one-shot Zenith Storm
- crafting tables and activation gating
- one-per-world Boss Blade locks
- destruction unlock handling

BUILD
=====
Extract -> upload everything inside -> replace old files -> Commit -> Actions.

If GitHub is red again, send the FIRST red compiler/error block.
