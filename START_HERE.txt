CHILL ZONE ZENITH - FIX 15
MINECRAFT 26.2 API COMPILE FIX

This is the SAME MOD moving forward from Fix 14.

THE ERRORS IN YOUR SCREENSHOT
=============================
1) ZenithCommands.java:
       level.getAllEntities().stream()
   Minecraft 26.2 returns an Iterable here, not a Java Stream.

2) ZenithAbilityScheduler.java:
       player.serverLevel()
   That method is not available in this 26.2 mapping/API.

WHAT I CHANGED
==============
- Replaced the dropped-item detector Stream code with a normal foreach loop.
- Replaced player.serverLevel() with player.level().
- Where a ServerLevel is required, the player's level is cast to ServerLevel.
- I searched the project for the exact broken calls and removed all remaining
  uses of serverLevel() and getAllEntities().stream().

NOTHING FROM THE MOD WAS REMOVED.
Fix 15 still contains:
- custom crafting tables and branch crafting
- activation/deactivation
- one-per-world Boss Blade crafting locks
- /zenith detect <category>
- /zenith resetboss <category>
- /zenith give <targets> <item> <count>
- dedicated Zenith Creative tab
- coloured names and lore/descriptions
- vanilla-style visual cooldowns
- Warden delayed targeting/beam prototype
- 15-lane one-shot Zenith Storm
- the rest of the sword ability prototypes

UPLOAD
======
1. Download and Extract All.
2. Upload EVERYTHING INSIDE the extracted folder to the SAME GitHub repository.
3. Replace the old files.
4. Commit changes.
5. Open Actions.

If it turns red again, send the FIRST red error block.
