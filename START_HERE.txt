CHILL ZONE ZENITH - FIX 3

This version fixes the Java compile errors shown after Fix 2.

FIXED:
- CommandSourceStack.hasPermission(int) was replaced with the Minecraft 26.2 permission API:
  source.permissions().hasPermission(Permissions.COMMANDS_GAMEMASTER)

- Old SavedData CompoundTag save/load overrides were removed.
- Progression persistence now uses Minecraft 26.2's Codec + SavedDataType API.
- The six branch switches are stored as one small integer bitmask.
- computeIfAbsent now receives the SavedDataType directly.

WHAT YOU DO:
1. Download this ZIP.
2. Extract All.
3. Open the extracted folder.
4. Upload EVERYTHING INSIDE it to the SAME GitHub repository.
5. Replace the old files when GitHub asks.
6. Commit changes.
7. Click Actions.
8. Open the newest "Build Chill Zone Zenith" run.

GREEN CHECK = send me a screenshot or download the artifact.
RED X = send me the FIRST error section again.

You do not need to edit Java yourself.
