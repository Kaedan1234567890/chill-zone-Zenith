CHILL ZONE ZENITH - FIX 13 / SYNTAX FIX

This is the SAME Fix 12 build with the Java syntax error corrected.

ERROR FIXED
===========
AbilitySwordItem.java line ~181:
    case, default, or '}' expected
    sonicTrail(level, user, 16.0);

Cause:
Two leftover lines from the OLD Warden Blade ability were accidentally left
underneath the NEW delayed Warden Blade block.

Those stale lines are now removed.

NOTHING ELSE WAS REMOVED.
Fix 13 still includes:
- /zenith detect <category>
- /zenith resetboss <category>
- targetable /zenith give <targets> <item> <count>
- custom names, colours, lore, ability descriptions and cooldowns
- dedicated Zenith Creative tab
- Warden Blade reveal -> 2.5 second aim window -> 15 block beam
- 15-lane one-shot Zenith Storm
- visual vanilla-style cooldown overlay
- crafting tables and activation gating
- one-per-world Boss Blade locks

BUILD
=====
Extract -> upload everything inside -> replace old files -> Commit -> Actions.

If GitHub is red again, send ChatGPT the FIRST red compiler/error block.
