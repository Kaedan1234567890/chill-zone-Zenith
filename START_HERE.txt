CHILL ZONE ZENITH - FIX 14
FULL ABILITY SYNTAX CLEANUP

This is the SAME MOD moving forward from Fix 13.

WHAT WAS WRONG
==============
Fix 13 removed the leftover lines from the OLD Warden Blade code, but there was
a SECOND leftover block from the OLD Zenith Storm implementation farther down
in AbilitySwordItem.java.

That stale block caused:
    case, default, or '}' expected
at about line 234.

WHAT I FIXED
============
- Removed the old duplicate Zenith particle block.
- Re-checked the ENTIRE main ability switch instead of only the reported line.
- Confirmed every ability case appears exactly once.
- Confirmed the previous stale Warden line is gone.
- Confirmed the stale Zenith block is gone.
- Confirmed the Java file's braces are balanced.

NOTHING ELSE WAS REMOVED
========================
Fix 14 still includes:
- /zenith detect <category>
- /zenith resetboss <category>
- /zenith give <targets> <item> <count>
- custom sword names, colours, lore and ability descriptions
- dedicated Zenith Creative tab
- visual vanilla-style cooldown overlay
- Warden Blade reveal -> aim delay -> beam prototype
- 15-lane one-shot Zenith Storm
- crafting and activation gating
- one-per-world Boss Blade locks
- automatic dropped-item destruction unlock handling

BUILD
=====
Extract -> upload everything inside -> replace old files -> Commit -> Actions.

If GitHub is red again, send the FIRST red compiler/error block.
